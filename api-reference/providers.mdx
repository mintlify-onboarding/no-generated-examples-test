---
title: "List of Providers"
openapi: "GET /v2/providers"
---

<RequestExample>

```bash Shell
curl --request GET \
     --url https://api.bazinga-api.com/v2/providers/ \
     --header 'Accept: application/json'
     --header 'x-bazinga-api-key: <API_KEY>' \
'
```

```javascript Node
const { BazingaClient } = require("Bazinga");

const client = new BazingaClient({
  api_key: <API_KEY>,
  environment: "sandbox",
  region: "us"
});

const user = await client.User.create(client_user_id);
```

```python Python
from bazinga import Client

client = Client(
  api_key=<API_KEY>,
  environment="sandbox",
  region="us"
)

user = client.User.create(client_user_id)
```

</RequestExample>

<ResponseExample>

```json Response
[
  {
    "name": "Oura",
    "slug": "oura",
    "logo": "https://logo_url.com",
    "auth_type": "oauth",
    "supported_resources": ["workouts", "sleep"]
  }
]
```

</ResponseExample>
